# Nodesmith Cosmos-hub

This is a small repository used for building and publishing the cli and node for the Cosmos Hub. The build produces a docker image which includes the `gaiacli` and `gaiad` executables.
